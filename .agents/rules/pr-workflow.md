# Antigravity Workspace Rule: PR Workflow

- This is an Antigravity workspace rule.
- The agent must work on a branch, not main.
- The agent must report diff and wait for approval before commit/push.
- deploy.bat is disabled and must not be run.
- No fake AI/data/UI.
- Failed AI must be explicit, not neutral fallback.
